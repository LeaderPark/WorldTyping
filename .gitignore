node_modules/
dist/
.wrangler/
.turbo/
*.log
.DS_Store
Thumbs.db
coverage/
.vite/
test-results/
playwright-report/
.playwright/
blob-report/
e2e/artifacts/
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# WT-M6-05: k6 부하 테스트가 매 실행마다 로컬 wrangler dev 대상으로 새로 발급하는 세션/runToken
# 픽스처(ephemeral, 30분 TTL) — 커밋 대상 아님(tooling/ops/loadtest-report.md에 결과만 남긴다).
tooling/ops/loadtest/.out/

# WT-HOST-01: Docker 자기호스팅 스택의 실 시크릿/터널 자격증명/백업 산출물 — 위 .env·.dev.vars
# 패턴과 일부 겹치지만(경로 무관 매치) 자기호스팅 전용 산출물임을 명시적으로 남겨둔다.
tooling/selfhost/.env
tooling/selfhost/cloudflared/*.json
tooling/selfhost/cloudflared/cert.pem
tooling/selfhost/backups/
tooling/selfhost/migration/
# WT-DBRESET-TOOL: reset-db.ps1의 자동 백업 산출물(수동 초기화 전 스냅샷) — 위 backups/(일일
# 자동 백업, 14일 롤링)와 별도 디렉터리라 그 정리 로직에 걸리지 않으므로 여기서 명시 제외한다.
tooling/selfhost/backup/manual/

# Claude Code 격리 worktree(전이적)
.claude/worktrees/
tooling/selfhost/deploy-logs/
