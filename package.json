{
  "name": "worldtyping",
  "private": true,
  "version": "0.0.0",
  "description": "WORLD TYPING (TypeTrip) monorepo root",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.21.0",
  "scripts": {
    "build:data": "tsx tooling/scripts/build-data.ts",
    "build:og-fonts": "node tooling/scripts/build-og-fonts.mjs",
    "build:moderation-snapshot": "tsx tooling/scripts/build-moderation-snapshot.ts",
    "contrast-check": "tsx tooling/ci/contrast-check.ts",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "vitest run && pnpm --filter @wt/api run test:workers",
    "build": "pnpm -r --if-present run build",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "e2e": "pnpm --filter @wt/e2e run e2e"
  },
  "devDependencies": {
    "@types/d3-geo": "^3.1.0",
    "@types/topojson-client": "^3.1.5",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "chrome-launcher": "^1.2.1",
    "d3-geo": "^3.1.1",
    "eslint": "^8.57.1",
    "eslint-plugin-import": "^2.31.0",
    "lighthouse": "^13.4.1",
    "pretendard": "1.3.9",
    "subset-font": "2.5.0",
    "topojson-client": "^3.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd",
      "sharp"
    ]
  }
}
